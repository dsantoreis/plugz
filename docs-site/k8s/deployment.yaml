apiVersion: apps/v1
kind: Deployment
metadata:
  name: plugz-docs
  labels:
    app: plugz-docs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: plugz-docs
  template:
    metadata:
      labels:
        app: plugz-docs
    spec:
      containers:
        - name: docs
          image: plugz-docs:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4321
          readinessProbe:
            httpGet:
              path: /
              port: 4321
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 4321
            initialDelaySeconds: 15
            periodSeconds: 20
