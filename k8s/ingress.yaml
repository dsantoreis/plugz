apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: skills-registry
spec:
  rules:
    - host: skills.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: skills-registry
                port:
                  number: 80
